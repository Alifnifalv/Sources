<div id="editrightpanel">
    <!--summary_quickview-->
    <div class="summary_quickview">
        <ul class="squick-view">
            <li ng-repeat="quickview in QuickSmartView track by $index" ng-click="OpenQuickSmartView($index)">
                {{quickview.Title}}
                <span class="list-close" ng-click="CloseQuickSmartView($index)">x</span>
            </li>
        </ul>
    </div>
</div>